<?xml version="1.0" encoding="utf-8"?>
<!--
  Notes (2026-09-26, fixed after two real parse failures):
  this file previously failed to load at all with "could not parse or
  process unattend answer file", root-caused to two bugs: double hyphens
  (forbidden anywhere inside an XML comment body, which this very note
  now deliberately avoids) in the old draft notes here, and the wcm
  namespace being used throughout (wcm:action="add") without ever being
  declared on the root element below. Confirmed well-formed now via
  xmllint and python's xml.dom.minidom.

  Remaining things to verify on the next real run:
    - ImageInstall/InstallFrom/MetaData Value below must match this
      specific ISO's edition names. List them with:
        Get-WindowsImage -ImagePath D:\sources\install.wim
      Deliberately targeting Server Core (SERVERSTANDARDCORE) not Desktop
      Experience (SERVERSTANDARD): every ctlabs qemu node is headless and
      SSH-managed already (QEMU_VGA=none convention), no GUI is ever used,
      and Core's footprint is roughly a third the size (about 4 to 6G vs
      10 to 13G used post-install). Swap back to SERVERSTANDARD if a lab
      ever genuinely needs the GUI (e.g. RDP into a desktop-like target).
    - Product key below is the public Microsoft KMS client setup key for
      Server 2022 Standard (safe to publish, does not activate anything by
      itself); swap it if targeting Datacenter or Core specifically.
    - winrm quickconfig timing: needs the network to be up when the
      specialize pass runs; if WinRM never comes up, this is the first
      place to look.
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend" xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">

  <settings pass="windowsPE">
    <component name="Microsoft-Windows-International-Core-WinPE" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <SetupUILanguage>
        <UILanguage>en-US</UILanguage>
      </SetupUILanguage>
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UserLocale>en-US</UserLocale>
    </component>
    <component name="Microsoft-Windows-PnpCustomizationsWinPE" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <DriverPaths>
        <PathAndCredentials wcm:action="add" wcm:keyValue="1">
          <!-- floppy_files flattens vioscsi + NetKVM's 2k22/amd64 files
               into this same root; see win2022.pkr.hcl. Without this,
               WinPE can't see the virtio-scsi disk at all, which is what
               actually caused "could not apply <DiskConfiguration>". -->
          <Path>A:\</Path>
        </PathAndCredentials>
      </DriverPaths>
    </component>
    <component name="Microsoft-Windows-Setup" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <DiskConfiguration>
        <Disk wcm:action="add">
          <DiskID>0</DiskID>
          <WillWipeDisk>true</WillWipeDisk>
          <CreatePartitions>
            <CreatePartition wcm:action="add">
              <Order>1</Order>
              <Type>Primary</Type>
              <Size>350</Size>
            </CreatePartition>
            <CreatePartition wcm:action="add">
              <Order>2</Order>
              <Type>Primary</Type>
              <Extend>true</Extend>
            </CreatePartition>
          </CreatePartitions>
          <ModifyPartitions>
            <ModifyPartition wcm:action="add">
              <Order>1</Order>
              <PartitionID>1</PartitionID>
              <Label>System</Label>
              <Format>NTFS</Format>
              <Active>true</Active>
            </ModifyPartition>
            <ModifyPartition wcm:action="add">
              <Order>2</Order>
              <PartitionID>2</PartitionID>
              <Label>OS</Label>
              <Format>NTFS</Format>
              <Letter>C</Letter>
            </ModifyPartition>
          </ModifyPartitions>
        </Disk>
        <WillShowUI>OnError</WillShowUI>
      </DiskConfiguration>
      <ImageInstall>
        <OSImage>
          <InstallFrom>
            <MetaData wcm:action="add">
              <Key>/IMAGE/NAME</Key>
              <Value>Windows Server 2022 SERVERSTANDARDCORE</Value>
            </MetaData>
          </InstallFrom>
          <InstallTo>
            <DiskID>0</DiskID>
            <PartitionID>2</PartitionID>
          </InstallTo>
        </OSImage>
      </ImageInstall>
      <UserData>
        <AcceptEula>true</AcceptEula>
        <!-- No ProductKey: this is evaluation media (WIM Edition ID
             ServerStandardEval/ServerDatacenterEval, confirmed via
             wimlib-imagex info 2026-09-26), pre-activated as a 180-day
             trial. A retail/volume KMS client setup key here doesn't match
             any Eval-SKU image, which is exactly what caused Setup's
             "No images are available": every image got filtered out. -->
      </UserData>
    </component>
  </settings>

  <settings pass="specialize">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <ComputerName>*</ComputerName>
      <TimeZone>Eastern Standard Time</TimeZone>
    </component>
    <component name="Microsoft-Windows-Deployment" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <RunSynchronous>
        <RunSynchronousCommand wcm:action="add">
          <Order>1</Order>
          <Description>Enable WinRM so Packer's communicator can connect</Description>
          <Path>cmd.exe /c winrm quickconfig -quiet</Path>
        </RunSynchronousCommand>
        <RunSynchronousCommand wcm:action="add">
          <Order>2</Order>
          <Description>Allow WinRM through the firewall</Description>
          <Path>cmd.exe /c netsh advfirewall firewall add rule name="WinRM 5985" dir=in action=allow protocol=TCP localport=5985</Path>
        </RunSynchronousCommand>
        <RunSynchronousCommand wcm:action="add">
          <Order>3</Order>
          <Description>Set network location to Private so WinRM/firewall rules take effect immediately</Description>
          <Path>cmd.exe /c powershell -Command "Get-NetConnectionProfile | Set-NetConnectionProfile -NetworkCategory Private"</Path>
        </RunSynchronousCommand>
        <RunSynchronousCommand wcm:action="add">
          <Order>4</Order>
          <Description>Enable Basic auth on WinRM -- the actual, root-caused fix (2026-09-26) for the "waiting for WinRM" stall</Description>
          <Path>cmd.exe /c winrm set winrm/config/service/auth @{Basic="true"}</Path>
        </RunSynchronousCommand>
        <RunSynchronousCommand wcm:action="add">
          <Order>5</Order>
          <Description>Basic auth's credentials aren't encrypted at the WinRM layer over plain HTTP, so the service refuses it unless this is set. Fine for a disposable, NAT-isolated build VM.</Description>
          <Path>cmd.exe /c winrm set winrm/config/service @{AllowUnencrypted="true"}</Path>
        </RunSynchronousCommand>
      </RunSynchronous>
    </component>
  </settings>

  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <NetworkLocation>Work</NetworkLocation>
        <ProtectYourPC>3</ProtectYourPC>
        <SkipMachineOOBE>true</SkipMachineOOBE>
        <SkipUserOOBE>true</SkipUserOOBE>
      </OOBE>
      <UserAccounts>
        <AdministratorPassword>
          <!-- overridden by build.sh -> keep in sync with var.admin_password -->
          <Value>ctlabs-BuildTime!1</Value>
          <PlainText>true</PlainText>
        </AdministratorPassword>
      </UserAccounts>
      <AutoLogon>
        <Enabled>true</Enabled>
        <LogonCount>1</LogonCount>
        <Username>Administrator</Username>
        <Password>
          <Value>ctlabs-BuildTime!1</Value>
          <PlainText>true</PlainText>
        </Password>
      </AutoLogon>
    </component>
  </settings>

</unattend>
